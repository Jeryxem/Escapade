namespace Escapade.gui
{
  /// <summary>
  /// A GuiEvent - for now this just determined which mouse button
  /// is being pressed so we can update object accordingly
  /// </summary>
  public enum GuiEvent
  {
    MouseLeft,
    MouseMiddle,
    MouseRight,
    LeftDown,
    MiddleDown,
    RightDown
  }
}